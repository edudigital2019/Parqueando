server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/parqueandodb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: parqueando
    password: parqueando

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  flyway:
    enabled: false

  security:
    user:
      name: admin
      password: admin123

  jackson:
    serialization:
      write-dates-as-timestamps: false

app:
  jwt:
    secret: ${JWT_SECRET:dev-secret-change-me}
    issuer: parqueando
    expirationMinutes: 180

  cloudinary:
    cloudName: dtq47e8zl
    apiKey: 593182226547828
    apiSecret: DiCML9juRTw3Jo0DWaA36ke1i-o

logging:
  level:
    org.hibernate.SQL: warn